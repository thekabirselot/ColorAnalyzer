# ColorAnalyzer
Pixel-level color distribution analyzer built in Python and NumPy — classifies every pixel in an uploaded image into 11 color categories (White, Black, Red, Blue, Brown, etc.) and renders inline pie and bar charts. Built end-to-end from a full product requirements document, taken from spec through to a working Colab prototype.
